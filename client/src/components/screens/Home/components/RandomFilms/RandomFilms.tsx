import { IMovie } from "@/types/IMovie";
import { SwiperSlide } from "swiper/react";
import { Carousel } from "@/components/Carousel/Carousel";

export const RandomFilms = () => {
    return (
        <div>
            <Carousel title="Случайное кино" quantity={3}>
                123
            </Carousel>
        </div>
    );
};
