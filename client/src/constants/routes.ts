export enum RoutesEnum {
  Home = "/",
  Films = "/films",
  // Series = "/series",
  // Cartoons = "/cartoons",
  Favorites = "/favorites",
  WatchLater = "/watchLater",
  Login = "/login",
  // Register = "/register",
}
