export enum RoutesEnum {
    Home = "/",
    // Films = "/films",
    // Series = "/series",
    // Cartoons = "/cartoons",
    Favourites = "/favorites",
    // Login = "/login",
    // Register = "/register",
}
